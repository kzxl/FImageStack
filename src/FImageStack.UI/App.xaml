<Application x:Class="FImageStack.UI.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <!-- Dark Theme Color Palette Tokens -->
        <Color x:Key="BgDarkColor">#181825</Color>
        <Color x:Key="BgPanelColor">#1E1E2E</Color>
        <Color x:Key="BgCardColor">#252538</Color>
        <Color x:Key="BgInputColor">#2E2E44</Color>
        <Color x:Key="BorderDefaultColor">#3E3E58</Color>
        <Color x:Key="PrimaryAccentColor">#89B4FA</Color>
        <Color x:Key="SuccessAccentColor">#A6E3A1</Color>
        <Color x:Key="WarningAccentColor">#F9E2AF</Color>
        <Color x:Key="DangerAccentColor">#F38BA8</Color>
        <Color x:Key="TextPrimaryColor">#CDD6F4</Color>
        <Color x:Key="TextMutedColor">#7F849C</Color>

        <SolidColorBrush x:Key="BgDark" Color="{StaticResource BgDarkColor}" />
        <SolidColorBrush x:Key="BgPanel" Color="{StaticResource BgPanelColor}" />
        <SolidColorBrush x:Key="BgCard" Color="{StaticResource BgCardColor}" />
        <SolidColorBrush x:Key="BgInput" Color="{StaticResource BgInputColor}" />
        <SolidColorBrush x:Key="BorderDefault" Color="{StaticResource BorderDefaultColor}" />
        <SolidColorBrush x:Key="PrimaryAccent" Color="{StaticResource PrimaryAccentColor}" />
        <SolidColorBrush x:Key="SuccessAccent" Color="{StaticResource SuccessAccentColor}" />
        <SolidColorBrush x:Key="WarningAccent" Color="{StaticResource WarningAccentColor}" />
        <SolidColorBrush x:Key="DangerAccent" Color="{StaticResource DangerAccentColor}" />
        <SolidColorBrush x:Key="TextPrimary" Color="{StaticResource TextPrimaryColor}" />
        <SolidColorBrush x:Key="TextMuted" Color="{StaticResource TextMutedColor}" />

        <!-- Base Window Style -->
        <Style TargetType="Window">
            <Setter Property="Background" Value="{StaticResource BgDark}" />
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}" />
            <Setter Property="FontFamily" Value="Segoe UI, Inter, Roboto, Arial" />
            <Setter Property="FontSize" Value="13" />
        </Style>

        <!-- Modern Button Style -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource BgCard}" />
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}" />
            <Setter Property="BorderBrush" Value="{StaticResource BorderDefault}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="12,7" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#32324A" />
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource PrimaryAccent}" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#25253A" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.45" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Primary Glow Action Button -->
        <Style x:Key="PrimaryActionButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="#313E5C" />
            <Setter Property="Foreground" Value="{StaticResource PrimaryAccent}" />
            <Setter Property="BorderBrush" Value="{StaticResource PrimaryAccent}" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Padding" Value="16,10" />
        </Style>

        <!-- Dark Tab Button Style -->
        <Style x:Key="TabRadioButton" TargetType="RadioButton">
            <Setter Property="Foreground" Value="{StaticResource TextMuted}" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Padding" Value="14,8" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="tabBorder" Background="{TemplateBinding Background}"
                                BorderThickness="0,0,0,2" BorderBrush="Transparent"
                                Padding="{TemplateBinding Padding}" CornerRadius="4,4,0,0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="{StaticResource TextPrimary}" />
                                <Setter TargetName="tabBorder" Property="Background" Value="#28283E" />
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter Property="Foreground" Value="{StaticResource PrimaryAccent}" />
                                <Setter TargetName="tabBorder" Property="BorderBrush" Value="{StaticResource PrimaryAccent}" />
                                <Setter TargetName="tabBorder" Property="Background" Value="{StaticResource BgCard}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Dark ComboBox Style -->
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="{StaticResource BgInput}" />
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}" />
            <Setter Property="BorderBrush" Value="{StaticResource BorderDefault}" />
            <Setter Property="Padding" Value="8,5" />
        </Style>

        <!-- Dark CheckBox Style -->
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Margin" Value="0,3" />
        </Style>

        <!-- Dark Slider Style -->
        <Style TargetType="Slider">
            <Setter Property="Foreground" Value="{StaticResource PrimaryAccent}" />
        </Style>

        <!-- Dark ProgressBar Style -->
        <Style TargetType="ProgressBar">
            <Setter Property="Background" Value="{StaticResource BgInput}" />
            <Setter Property="Foreground" Value="{StaticResource PrimaryAccent}" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Height" Value="6" />
        </Style>
    </Application.Resources>
</Application>
